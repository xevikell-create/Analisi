/* V2.1 stability patch */
(function(){function boot(){if(typeof pages==='undefined'||typeof data==='undefined'||typeof total==='undefined'||typeof save==='undefined'){setTimeout(boot,100);return;}pages.riesgo=function(){const rows=(typeof I!=='undefined'&&I?.rows?.length?I.rows:[]).filter(r=>Number(r.valueEUR)>0);const inv=rows.reduce((s,r)=>s+Number(r.valueEUR||0),0);const liquidity=typeof liq==='function'?Number(liq())||0:0;const eth=Number((typeof I!=='undefined'&&I?.eth?.valueEUR)||0);const patrimonio=inv+liquidity+eth;const sorted=rows.map(r=>({name:r.name,value:Number(r.valueEUR)||0})).sort((a,b)=>b.value-a.value);const top=sorted[0]||{name:'Sin datos',value:0};const topWeight=patrimonio?top.value/patrimonio:0;const equityWeight=patrimonio?inv/patrimonio:0;const cryptoWeight=patrimonio?eth/patrimonio:0;const level=topWeight>.25||cryptoWeight>.15?'ALTO':topWeight>.15||cryptoWeight>.10?'MEDIO':'CONTROLADO';const cls=level==='ALTO'?'red':level==='MEDIO'?'amber':'green';document.getElementById('app').innerHTML=`<h2>⚖️ Riesgo</h2><div class="grid"><div class="card"><span class="muted">Nivel global</span><div class="score"><span class="badge ${cls}">${level}</span></div><p class="small">Calculado automáticamente con la distribución sincronizada.</p></div><div class="card">Mayor posición<div class="kpi">${esc(top.name)}</div><span>${pct(topWeight)} del patrimonio</span></div><div class="card">Inversiones<div class="kpi">${pct(equityWeight)}</div><span>${money(inv)}</span></div><div class="card">Ethereum<div class="kpi">${pct(cryptoWeight)}</div><span>${money(eth)}</span></div></div><div class="card section"><h3>Concentración</h3>${sorted.length?sorted.slice(0,8).map(r=>`<div class="row"><span><b>${esc(r.name)}</b></span><span>${money(r.value)} · ${pct(patrimonio?r.value/patrimonio:0)}</span></div>`).join(''):'<div class="empty">Sin posiciones sincronizadas todavía.</div>'}</div>`};let tries=0;function autoSync(){if(typeof sync==='function'&&!window.__v4InitialSync){window.__v4InitialSync=true;sync()}else if(tries++<100)setTimeout(autoSync,100)}autoSync()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot()})();